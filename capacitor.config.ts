import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sancho.rick',
  appName: 'rickyandmorty-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
